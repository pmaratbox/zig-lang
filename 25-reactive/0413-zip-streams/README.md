# 0413 — Zip Streams

Implement zip that pairs values by index and combines them; zip [1,2,3] with [10,20,30] using a+b. Idiomatic Zig uses per-source `std.ArrayListUnmanaged` queues drained whenever both hold a value.

## Run

    zig run main.zig
