# 0409 — Distinct Until Changed

Implement distinctUntilChanged, dropping consecutive duplicate values from 1,1,2,2,2,3,1. An optional `?i64` field holds the last emitted value, forwarding only when the new value differs.

## Run

    zig run main.zig
