# 0417 — BehaviorSubject

Implement a BehaviorSubject that holds a current value and replays it immediately to each new subscriber. An ArrayListUnmanaged of observers takes the allocator on each append, idiomatic to Zig 0.16.

## Run

    zig run main.zig 2>&1
