# 0076 — Count Vowels

Count the vowels (`a`, `e`, `i`, `o`, `u`) in the word `hello` and print the total: `2`. `std.mem.indexOfScalar` returns an optional index; a non-null result means the byte is one of the vowels.

## Run

    zig run main.zig
