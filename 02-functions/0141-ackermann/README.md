# 0141 — Ackermann Function

Compute the Ackermann function A(2,3) recursively and print `9`. The classic doubly-recursive definition maps cleanly onto Zig's `if` expressions.

## Run

    zig run main.zig
