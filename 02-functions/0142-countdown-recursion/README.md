# 0142 — Countdown by Recursion

Recursively count down from 5 to 1, printing `5 4 3 2 1` on one line. Each call prints its value and recurses, omitting the trailing space on the last element.

## Run

    zig run main.zig
