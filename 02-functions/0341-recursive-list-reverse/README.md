# 0341 — Recursive List Reverse

Reverse [1,2,3] using recursion (no built-in reverse), printing `3 2 1`. In Zig, recurse over a slice and write the last element first into an output buffer.

## Run

    zig run main.zig
