# 0171 — Doubly Linked List

Build a doubly linked list 1<->2<->3, traverse forward then backward, printing `1 2 3` then `3 2 1` on two lines. Each `Node` holds optional `?*Node` prev/next pointers, and stack-allocated nodes are wired together by taking their addresses.

## Run

    zig run main.zig
