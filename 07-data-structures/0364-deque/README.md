# 0364 — Double-Ended Queue

Using a deque, push 1 and 2 to the back and 0 to the front, then print front-to-back `0 1 2`. An `ArrayListUnmanaged` models the deque: `append` pushes the back and `insert(0, ...)` pushes the front.

## Run

    zig run main.zig
