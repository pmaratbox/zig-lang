# 0177 — Min Heap (Manual)

Implement a binary min-heap by hand, push 3,1,2, pop all, printing `1 2 3`. A fixed array backs the heap, with `push` sifting up and `pop` sifting down to keep the minimum at index 0.

## Run

    zig run main.zig
