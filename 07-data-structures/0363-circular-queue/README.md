# 0363 — Circular Queue

In a capacity-3 circular queue enqueue 1,2,3, dequeue once, enqueue 4, then print the contents `2 3 4`. A fixed `[3]i32` array with `head`/`len` indices wraps via modular arithmetic.

## Run

    zig run main.zig
