# 0093 — Matrix Multiply

Multiply the 2x2 matrices `[[1, 2], [3, 4]]` and `[[5, 6], [7, 8]]` and print the product, one row per line: `19 22` and `43 50`. Fixed `[2][2]i32` matrices; the triple `for (0..2)` loop accumulates each `result[i][j]` dot product.

## Run

    zig run main.zig
