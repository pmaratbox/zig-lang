# 0088 — String Builder

Use a string builder to assemble `1`, `2`, and `3` joined by `-`, printing `1-2-3`. An unmanaged `ArrayList(u8)` is the byte buffer; `append`/`appendSlice` take the allocator, and `bufPrint` formats each number first.

## Run

    zig run main.zig
