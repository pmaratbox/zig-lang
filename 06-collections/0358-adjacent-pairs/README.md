# 0358 — Adjacent Pairs

Produce the consecutive pairs of [1,2,3,4], printing `1,2 2,3 3,4`. A `while` loop slides a size-2 window by indexing `nums[i]` and `nums[i+1]` while `i+1 < len`.

## Run

    zig run main.zig
