# 0168 — List Difference

Compute [1,2,3,4] minus the elements of [2,4], printing `1 3`. Iterating the first list and skipping any element found in the second preserves the original order.

## Run

    zig run main.zig
