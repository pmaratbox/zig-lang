# 0360 — Interleave Lists

Interleave [1,3,5] and [2,4,6] element by element, printing `1 2 3 4 5 6`. Zig's multi-object `for` walks both arrays in lockstep, emitting one element from each per step.

## Run

    zig run main.zig
