# 0167 — Top-K Frequent

Find the 2 most frequent elements of [a,b,a,c,b,a], printing `a b`. Tallying counts in first-seen order then selection-sorting by count descending surfaces the top-k without extra allocation.

## Run

    zig run main.zig
