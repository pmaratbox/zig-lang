# 0063 — Flatten Nested List

Flatten the nested list `[[1, 2], [3, 4]]` into a single flat sequence and print its elements: `1 2 3 4`. Zig flattens the array of fixed-size arrays by nested iteration — there is no library flatten for this.

## Run

    zig run main.zig
