# 0166 — Cartesian Product

Print the cartesian product of [1,2] and ["a","b"] as `1a 1b 2a 2b`. Nested `for` loops with the first list as the outer loop enumerate every pair in row-major order.

## Run

    zig run main.zig
