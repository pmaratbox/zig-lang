# 0163 — Take While

From [1,2,3,4,1] take elements while they are < 3, printing `1 2`. A `for` loop with an early `break` stops at the first failing element idiomatically in Zig.

## Run

    zig run main.zig
