# 0357 — Zip Longest

Zip [1,2,3] with ["a","b"], filling the missing slot with "-", printing `1a 2b 3-`. A `while` loop runs to the longer length, checking each index bound to substitute "-" when a list is exhausted.

## Run

    zig run main.zig
