# 0083 — Sliding Window

Slide a window of size `2` over `1, 2, 3, 4` and print each window on its own line: `1 2`, `2 3`, `3 4`. The outer `while` advances the start while a full window fits; the inner loop prints that window's elements.

## Run

    zig run main.zig
