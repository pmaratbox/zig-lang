# 0181 — Insertion Sort

Insertion-sort [5,1,4,2,8] ascending, printing `1 2 4 5 8`. Shift larger elements right and insert, using a `while` loop guarded so the `usize` index never underflows.

## Run

    zig run main.zig
