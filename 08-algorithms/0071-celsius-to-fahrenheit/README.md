# 0071 — Celsius to Fahrenheit

Convert `100` and `0` degrees Celsius to Fahrenheit using `F = C * 9 / 5 + 32`, printing `100C = 212F` and `0C = 32F`. `@divTrunc` is integer division truncating toward zero; `c * 9` is computed before dividing by 5.

## Run

    zig run main.zig
