# 0182 — Primality Test

Test whether 7 (prime) and 9 (not) are prime, printing `yes no`. Trial division up to sqrt(n) is expressed as `i * i <= n` to avoid a floating-point square root.

## Run

    zig run main.zig
