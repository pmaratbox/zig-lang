# 0094 — Linear Search

Linearly scan the array `4, 2, 7, 1, 9` for `7` and print the index where it is found: `found 7 at index 2`. `std.mem.indexOfScalar` scans the slice for the first element equal to the target, returning `?usize`.

## Run

    zig run main.zig
