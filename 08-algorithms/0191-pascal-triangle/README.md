# 0191 — Pascal Triangle

Print the first 4 rows of Pascal’s triangle, each row space-separated. Each entry is the sum of the two above; print 4 rows, updating one row buffer right-to-left in place.

## Run

    zig run main.zig
