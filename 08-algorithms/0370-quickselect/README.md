# 0370 — Quickselect

Find the 3rd smallest element of [7,10,4,3,20,15] with quickselect, printing `7`. In Zig a Lomuto partition recurses into a single side via a loop over a mutable slice.

## Run

    zig run main.zig
