# 0189 — Permutations

Generate all permutations of [1,2,3] in lexicographic order, one per line space-separated. Lexicographic order exactly as shown, produced by an in-place next-permutation step using `std.mem.swap` and `std.mem.reverse`.

## Run

    zig run main.zig
