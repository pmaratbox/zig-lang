# 0183 — Prime Factorization

Factor 60 into primes, printing `2 2 3 5`. Divide out 2s, then odd factors; print with multiplicity ascending, using `@divExact` since each division is known to be even.

## Run

    zig run main.zig
