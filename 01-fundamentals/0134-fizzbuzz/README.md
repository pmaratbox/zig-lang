# 0134 — FizzBuzz

Print 1..15, replacing multiples of 3 with Fizz, of 5 with Buzz, and of both with FizzBuzz, one per line. A `while` loop with a continue expression `: (i += 1)` is the idiomatic Zig counting loop.

## Run

    zig run main.zig
