# 0337 — Multiplication Table

Print the 3x3 multiplication table, each row space-separated. Nested `while` loops emit `i*j` with a separator before each column past the first.

## Run

    zig run main.zig
