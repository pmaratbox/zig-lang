# 0246 — Perfect Number Check

Test whether 6 (yes) and 8 (no) are perfect numbers, printing `yes no`. Zig's `if` expression selects the `"yes"`/`"no"` string slices inline.

## Run

    zig run main.zig
