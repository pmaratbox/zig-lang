# 0240 — Complex Multiplication

Multiply the complex numbers (1+2i) and (3+4i), printing the real and imaginary parts `-5 10`. Plain `i64` fields hold the real and imaginary parts in Zig.

## Run

    zig run main.zig
