# 0243 — Newton Square Root

Approximate sqrt(2) with Newton iteration and print it to 4 decimals `1.4142`. Zig's `{d:.4}` format specifier fixes the fractional precision.

## Run

    zig run main.zig
