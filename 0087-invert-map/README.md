# 0087 — Invert a Map

Invert the map `{a: 1, b: 2, c: 3}` (swap keys and values) and print the result sorted by the new key: `1:a 2:b 3:c`. Scanning the values `1..3` in order and printing `value:key` emits the inverted map already sorted by the new key.

## Run

    zig run main.zig
