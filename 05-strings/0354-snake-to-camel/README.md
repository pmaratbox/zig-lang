# 0354 — Snake to Camel Case

Convert "hello_world" to camelCase `helloWorld`. Zig walks the bytes, dropping underscores and upper-casing the next letter via an ASCII offset.

## Run

    zig run main.zig
