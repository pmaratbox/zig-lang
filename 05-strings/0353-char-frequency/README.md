# 0353 — Character Frequency

Count character frequencies in "aab" and print them in first-seen order `a:2 b:1`. Zig parallel arrays preserve insertion order while tallying counts.

## Run

    zig run main.zig
