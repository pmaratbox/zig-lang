# 0079 — Character Codes

Print the character code of `A` and the character for code `66`: `65 B`. A character literal `'A'` is a comptime integer; `{d}` formats the byte as a number and `{c}` formats it as a character.

## Run

    zig run main.zig
