# 0157 — Longest Common Prefix

Find the longest common prefix of ["flower","flow","flight"], printing `fl`. A labeled `break :outer` stops the column-by-column scan at the first mismatched byte.

## Run

    zig run main.zig
