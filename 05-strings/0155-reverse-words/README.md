# 0155 — Reverse Words

Reverse the order of words in "hello world", printing `world hello`. `std.mem.splitBackwardsScalar` walks the words from the end, so joining them yields the reversed order.

## Run

    zig run main.zig
