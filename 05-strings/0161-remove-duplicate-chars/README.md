# 0161 — Remove Duplicate Characters

Remove duplicate characters from "aabbcc" keeping first occurrence, printing `abc`. A 256-entry boolean seen-set tracks bytes already emitted while preserving first-seen order.

## Run

    zig run main.zig
