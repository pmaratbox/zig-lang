# 0072 — Palindrome Check

Check whether each of `level` and `hello` reads the same forwards and backwards, printing `level: yes` and `hello: no`. Two `usize` indices close in from the ends; `i += 1` is the loop's continue-expression and `j -= 1` runs in the body.

## Run

    zig run main.zig
