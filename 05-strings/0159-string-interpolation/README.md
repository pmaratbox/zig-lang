# 0159 — String Interpolation

Interpolate name="Ada" and age=36 into a sentence, printing `Ada is 36`. Zig has no string interpolation; the format string with `{s}` and `{d}` placeholders fills the same role.

## Run

    zig run main.zig
