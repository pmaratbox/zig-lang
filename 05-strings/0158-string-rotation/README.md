# 0158 — String Rotation Check

Check whether "cdab" is a rotation of "abcd", printing `yes`. `b` is a rotation of `a` iff they share a length and `std.mem.indexOf` finds `b` inside `a ++ a`.

## Run

    zig run main.zig
