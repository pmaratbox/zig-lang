# 0160 — Most Frequent Character

Find the most frequent character in "hello", printing `l`. A fixed 256-entry byte-count array tallies each character, then a left-to-right scan picks the first maximum.

## Run

    zig run main.zig
