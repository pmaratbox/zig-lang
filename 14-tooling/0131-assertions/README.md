# 0131 — Assertions

Assert several expected equalities (all true) and print `all passed`. In Zig, `std.debug.assert` panics on a false condition, so reaching the print means every check held.

## Run

    zig run main.zig
