# 0133 — Debug Representation

Produce a debug/structured representation of a Point(1,2) and print `Point(x=1, y=2)`. A custom `format` method on the struct lets the `{f}` specifier render it idiomatically.

## Run

    zig run main.zig
