# 0132 — Mini Test Runner

Run three named test functions that all pass and report `3 passed, 0 failed`. Here an array of `*const fn () bool` function pointers is iterated, tallying results idiomatically.

## Run

    zig run main.zig
