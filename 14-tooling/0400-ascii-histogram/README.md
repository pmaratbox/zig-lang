# 0400 — ASCII Histogram

Render a histogram of [3,1,2] as rows of "#" characters, one bar per line. A simple nested `while` over each value emits that many `#` then a newline.

## Run

    zig run main.zig
