# 0374 — Error Codes

Map the numeric error code 2 to its message "not found" and print `not found`. A Zig `switch` over the code value gives an exhaustive, branch-checked lookup.

## Run

    zig run main.zig
