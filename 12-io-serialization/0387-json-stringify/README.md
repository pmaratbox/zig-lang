# 0387 — JSON Stringify

Serialize the object {name:"Ada", age:36} to compact JSON `{"name":"Ada","age":36}`. In Zig, escape braces as `{{`/`}}` in the format string to emit literal JSON delimiters.

## Run

    zig run main.zig
