# 0388 — JSON Parse

Parse the JSON object `{"x":1,"y":2}` and print its entries `x=1 y=2`. In Zig, slice the string by scanning for quotes and digits to extract each key/value.

## Run

    zig run main.zig
