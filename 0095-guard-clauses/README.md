# 0095 — Guard Clauses

Classify each of `-1`, `0`, and `5` using guard clauses (an early return for each special case) and print `negative`, `zero`, and `positive`. Each guard returns a string slice early; the final `return` covers the remaining case.

## Run

    zig run main.zig
