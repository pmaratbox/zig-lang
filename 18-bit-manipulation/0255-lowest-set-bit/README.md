# 0255 — Lowest Set Bit

Isolate the lowest set bit of 12 (1100), printing `4`. On unsigned ints use wrapping negation (~x +% 1) since Zig has no signed two's-complement negation of unsigned.

## Run

    zig run main.zig
