# 0260 — Pack and Unpack Bits

Pack r=1,g=2,b=3 into one integer (8 bits each) then unpack them, printing `1 2 3`. Shifts and 0xff masks pack and recover each byte-sized field.

## Run

    zig run main.zig
