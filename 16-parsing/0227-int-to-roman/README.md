# 0227 — Integer to Roman

Format the integer 14 as the Roman numeral `XIV`. Parallel value/symbol arrays iterated with a multi-object `for` greedily subtract idiomatically.

## Run

    zig run main.zig
