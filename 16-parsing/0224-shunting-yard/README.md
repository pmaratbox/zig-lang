# 0224 — Shunting Yard

Convert the infix expression "3 + 4 * 2" to postfix `3 4 2 * +`. An operator stack with precedence comparison emits the RPN tokens idiomatically.

## Run

    zig run main.zig
