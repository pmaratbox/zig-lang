# 0221 — Tokenizer

Tokenize the expression "1 + 2" into token types, printing `NUM PLUS NUM`. A `switch` over each character maps digits and "+" to token-type names idiomatically.

## Run

    zig run main.zig
