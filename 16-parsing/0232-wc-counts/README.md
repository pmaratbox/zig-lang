# 0232 — Word/Line/Char Counts

For the text "a b\nc" report words, lines, and characters, printing `3 2 5`. `tokenizeAny` counts words while a newline scan counts lines idiomatically.

## Run

    zig run main.zig
