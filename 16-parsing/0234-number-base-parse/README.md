# 0234 — Parse Number Bases

Parse "ff" as hexadecimal (255) and "101" as binary (5), printing `255 5`. `std.fmt.parseInt` with an explicit radix converts each digit string idiomatically.

## Run

    zig run main.zig
