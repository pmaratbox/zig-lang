# 0230 — Parse JSON Array

Parse the JSON array text "[1,2,3]" and print the sum of its elements `6`. Slicing off the brackets and `tokenizeScalar` on commas parses each int idiomatically.

## Run

    zig run main.zig
