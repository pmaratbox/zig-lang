# 0225 — Recursive Descent Calculator

Parse and evaluate "2+3*4" honoring precedence, printing `14`. A `Parser` struct with `expr`/`term`/`factor` methods encodes the grammar idiomatically.

## Run

    zig run main.zig
