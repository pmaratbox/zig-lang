# 0342 — Tagged Union Eval

Model an expression tagged union Num | Add and evaluate Add(Num 1, Num 2), printing `3`. Zig's `union(enum)` gives a tagged sum type switched exhaustively.

## Run

    zig run main.zig
