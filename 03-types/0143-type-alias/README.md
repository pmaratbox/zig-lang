# 0143 — Type Alias

Define a semantic alias for an integer type (e.g. Meters), assign 5, and print `distance: 5`. In Zig a type is a value, so `const Meters = i64;` makes a compile-time alias.

## Run

    zig run main.zig
