# 0013 — Optional

Hold one value that is present (`42`) and one that is absent, then print each
with a fallback of `-1` when absent. An optional type is written `?i32`, holds a
value or `null`, and the `orelse` operator supplies the fallback.

## Run

    zig run main.zig
