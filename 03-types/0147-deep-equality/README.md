# 0147 — Deep Equality

Compare two nested structures with equal contents for structural equality and print `equal: yes`. `std.meta.eql` recursively compares structs field by field at compile time.

## Run

    zig run main.zig
