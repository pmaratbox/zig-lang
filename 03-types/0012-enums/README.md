# 0012 — Enums

Define a `Color` enum (`red`, `green`, `blue`) and print the integer value of
`green` (1) and `blue` (2). Zig enum tags auto-number from 0; `@intFromEnum`
converts a tag to its integer value (and `@tagName` would give the name).

## Run

    zig run main.zig
