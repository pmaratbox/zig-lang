# 0145 — Nested Struct

Model a person whose address is a nested struct, and print the city `London`. Zig structs nest by value, and `.{ ... }` infers the nested struct's type from context.

## Run

    zig run main.zig
