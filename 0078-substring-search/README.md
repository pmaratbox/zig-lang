# 0078 — Substring Search

Find the index of the first occurrence of `lo` in `hello` and print it: `3`. `std.mem.indexOf` returns `?usize` — the byte index of the first match, or `null`; `.?` unwraps the present value.

## Run

    zig run main.zig
