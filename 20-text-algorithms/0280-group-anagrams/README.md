# 0280 — Group Anagrams

Group ["eat","tea","tan","ate","nat"] into anagram classes and print the number of groups `2`. Zig sorts each word's bytes with std.sort.asc to form its key.

## Run

    zig run main.zig
