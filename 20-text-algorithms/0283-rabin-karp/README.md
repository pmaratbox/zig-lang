# 0283 — Rabin-Karp Search

Use rolling-hash Rabin-Karp to find all start indices of "ab" in "xabxab", printing `1 4`. Zig does the modular rolling hash in u64 arithmetic.

## Run

    zig run main.zig
