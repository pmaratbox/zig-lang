# 0273 — KMP Search

Use Knuth-Morris-Pratt to find all start indices of "ab" in "ababab", printing `0 2 4`. Zig builds the prefix-function in a fixed-size array on the stack.

## Run

    zig run main.zig
