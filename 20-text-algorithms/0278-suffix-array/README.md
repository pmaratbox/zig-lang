# 0278 — Suffix Array

Build the suffix array of "banana" (sorted suffix start indices), printing `5 3 1 0 4 2`. Zig sorts the index array with std.mem.sort and a std.mem.lessThan comparator.

## Run

    zig run main.zig
