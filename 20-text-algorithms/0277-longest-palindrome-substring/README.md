# 0277 — Longest Palindromic Substring

Find the longest palindromic substring of "babad" (first of maximal length), printing `bab`. Zig returns an anonymous struct from the expand-around-center helper.

## Run

    zig run main.zig
