# 0100 — Static Methods

Define a static (class-level) method `square` that needs no instance, call it as `square(6)`, and print `36`. A function declared inside a `struct` with no `self` parameter is namespaced under it, called as `MathUtil.square(...)`.

## Run

    zig run main.zig
