# 0351 — Instance Counter

Track how many instances of a class have been created; after building three, print `3`. A container-level `var count` inside the struct acts as a static field, bumped by each `init`.

## Run

    zig run main.zig
