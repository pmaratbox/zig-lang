# 0099 — Method Chaining

Build a calculator that supports method chaining — start at `5`, then `.add(3).multiply(2)` — and print the result: `16`. Each method takes `self` by value and returns a new `Calc`, so calls chain with method-call syntax on the returned struct.

## Run

    zig run main.zig
