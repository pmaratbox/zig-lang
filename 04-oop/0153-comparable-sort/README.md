# 0153 — Comparable Sort

Sort people [(alice,30),(bob,25)] by age ascending and print their names `bob alice`. `std.mem.sort` takes a less-than comparator function, so ordering by a field is just a one-line predicate.

## Run

    zig run main.zig
