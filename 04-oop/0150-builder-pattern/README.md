# 0150 — Builder Pattern

Use a fluent builder to construct a Pizza of size M with cheese, and print `Pizza(M, cheese)`. Each builder method takes `self` by value and returns an updated copy, so calls chain fluently before `build()`.

## Run

    zig run main.zig
