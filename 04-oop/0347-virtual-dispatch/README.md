# 0347 — Virtual Dispatch

Store a Circle, Square, and Triangle in one Shape collection and call describe() on each, printing `circle square triangle`. A `Shape` struct holding a function pointer gives runtime dynamic dispatch over a base-typed array.

## Run

    zig run main.zig
