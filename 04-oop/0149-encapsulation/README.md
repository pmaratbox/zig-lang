# 0149 — Encapsulation

Keep an account balance private, deposit 50 onto an initial 100 through a method, and print `150`. Zig has no access modifiers, so encapsulation is expressed by mutating state only through methods on the struct.

## Run

    zig run main.zig
