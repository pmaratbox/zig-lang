# 0129 — Add Days to a Date

Add 40 days to 2000-01-01 and print the result as `2000-02-10`. Round-tripping through days-from-civil and back keeps the arithmetic integer-only, and `{d:0>4}` zero-pads the fields.

## Run

    zig run main.zig
