# 0127 — Day of Week (Zeller)

Use Zeller’s congruence to compute the weekday of 2000-01-01 and print `Saturday`. Zig's `@mod` and `@divFloor` give the floored arithmetic the formula needs for negative intermediates.

## Run

    zig run main.zig
