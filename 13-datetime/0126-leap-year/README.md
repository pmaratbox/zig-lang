# 0126 — Leap Year Check

Determine whether 2000, 1900, and 2024 are leap years, printing `yes no yes`. Zig's `%` modulo on unsigned ints composes the leap-year rule directly in a `bool` expression.

## Run

    zig run main.zig
