# 0393 — Weekday After N Days

Given 2000-01-01 is a Saturday, compute the weekday 3 days later, printing `Tuesday`. Modular arithmetic over a string slice array yields the name in Zig.

## Run

    zig run main.zig
