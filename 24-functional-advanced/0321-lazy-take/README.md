# 0321 — Lazy Take

Build a lazy sequence of the natural numbers and take the first five, printing `1 2 3 4 5`. A stateful struct with a `take` method models a lazy generator, pulling one value at a time.

## Run

    zig run main.zig
