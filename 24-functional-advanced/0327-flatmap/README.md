# 0327 — FlatMap

FlatMap [1,2,3] with x -> [x, x*10] and print the flattened result `1 10 2 20 3 30`. Each element expands into a two-element pair appended into a flat output buffer.

## Run

    zig run main.zig
