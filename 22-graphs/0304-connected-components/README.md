# 0304 — Connected Components

Count the connected components of the undirected graph with edges (0,1),(1,2),(3,4) over nodes 0..4, printing `2`. A Zig union-find counts roots that remain their own parent.

## Run

    zig run main.zig 2>&1
