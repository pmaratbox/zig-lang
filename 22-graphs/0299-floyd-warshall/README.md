# 0299 — Floyd-Warshall

Run all-pairs shortest paths on 0->1(3),1->2(1),0->2(5) and print the distance from 0 to 2 `4`. A Zig fixed 2-D array holds the dist matrix updated in place by the triple loop.

## Run

    zig run main.zig 2>&1
