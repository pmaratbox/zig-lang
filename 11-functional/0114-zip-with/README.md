# 0114 — Zip With

Combine [1,2,3] and [4,5,6] element-wise with addition, printing `5 7 9`. Zig's multi-object `for` loop iterates two slices in lockstep, summing each pair while a third range index controls the separator.

## Run

    zig run main.zig
