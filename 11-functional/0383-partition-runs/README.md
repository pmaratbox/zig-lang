# 0383 — Partition Into Runs

Partition [1,1,2,3,3,3] into runs of equal adjacent elements, printing `1 1|2|3 3 3`. A while loop scans forward over equal neighbours to delimit each run.

## Run

    zig run main.zig
