# 0213 — Iterator (Custom)

Implement a custom iterator over a range 1..3 and consume it, printing `1 2 3`. A struct exposes `hasNext()`/`next()` and advances its internal cursor on each call.

## Run

    zig run main.zig
