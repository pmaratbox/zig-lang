# 0210 — Strategy

Select an "add" then a "mul" strategy and apply each to (3,4), printing `7 12`. Strategies are interchangeable function pointers chosen at runtime by name.

## Run

    zig run main.zig
