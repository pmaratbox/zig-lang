# 0207 — Composite

Sum a composite tree of leaf values 1, 2, 3 through a uniform size() interface, printing `6`. A tagged union models leaf and composite nodes that share one recursive `size()`.

## Run

    zig run main.zig
