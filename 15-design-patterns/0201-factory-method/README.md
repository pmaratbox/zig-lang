# 0201 — Factory Method

Use a factory method to create shapes by name and print their kinds `circle square`. A simple factory function returns a struct whose `kind` field carries the name.

## Run

    zig run main.zig
