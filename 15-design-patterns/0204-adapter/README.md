# 0204 — Adapter

Adapt a Celsius source (100) to a Fahrenheit interface, printing `212`. The adapter struct wraps the Celsius value and exposes a `fahrenheit()` method.

## Run

    zig run main.zig
