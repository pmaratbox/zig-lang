# 0202 — Abstract Factory

Use a "dark" theme abstract factory to build a button and a checkbox, printing `dark-button dark-checkbox`. A `ThemeFactory` struct produces widgets whose labels carry the theme prefix.

## Run

    zig run main.zig
