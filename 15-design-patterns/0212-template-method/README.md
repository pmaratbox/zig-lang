# 0212 — Template Method

Run an algorithm skeleton whose middle step is overridden, printing `start work end` on one line. The fixed `run()` template calls an injected `step` function pointer for the variable middle.

## Run

    zig run main.zig
