# 0219 — Bridge

Bridge a Circle abstraction to a Red color implementation, printing `red circle`. The shape abstraction holds a color implementor and composes its `name()` into the description.

## Run

    zig run main.zig
