# 0271 — DFA String Match

Build a DFA for the pattern "ab" and report the first match index in "aab", printing `1`. The DFA's state tracks the count of matched pattern characters, advancing over the text in a Zig `for` loop.

## Run

    zig run main.zig
