# 0267 — Game of Life Step

Advance one step of a vertical blinker on a 3x3 grid (it becomes horizontal); print the grid using . and #. A fixed-size `[3][3]u8` grid with a neighbor count applies Conway's rules in plain Zig loops.

## Run

    zig run main.zig
