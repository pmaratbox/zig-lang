# 0265 — Vending Machine

Insert coins 10, 10, 5 to reach the price 25 and dispense, printing `dispensed`. A simple accumulator over a Zig array drives the state change once the total crosses the price.

## Run

    zig run main.zig
