# 0287 — 0/1 Knapsack

Maximize value within capacity 5 for items (w,v)=(2,3),(3,4),(4,5), printing `7`. A reverse capacity sweep over a 1-D Zig array prevents reusing an item.

## Run

    zig run main.zig 2>&1
