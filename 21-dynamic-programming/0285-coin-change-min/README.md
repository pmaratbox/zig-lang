# 0285 — Coin Change (Min Coins)

Find the fewest coins from {1,2,5} summing to 11, printing `3`. A fixed-size Zig array initialized with `maxInt` sentinels models the bottom-up DP table.

## Run

    zig run main.zig 2>&1
