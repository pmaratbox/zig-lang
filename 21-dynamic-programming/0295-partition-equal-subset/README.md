# 0295 — Equal Partition

Decide whether [1,5,11,5] can split into two equal-sum subsets, printing `yes`. Reducing to a half-total subset-sum check on a boolean Zig array settles it.

## Run

    zig run main.zig 2>&1
