# 0292 — House Robber

Maximize the sum of non-adjacent picks from [2,7,9,3,1], printing `12`. Two scalars track the rob/skip states as the Zig `for` loop walks the slice.

## Run

    zig run main.zig 2>&1
