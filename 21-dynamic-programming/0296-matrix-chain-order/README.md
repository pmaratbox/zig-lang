# 0296 — Matrix Chain Order

Find the minimum scalar multiplications to multiply matrices with dimensions [10,20,30,40], printing `18000`. A 2-D fixed Zig array filled by increasing chain length encodes the split DP.

## Run

    zig run main.zig 2>&1
