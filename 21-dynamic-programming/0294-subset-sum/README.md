# 0294 — Subset Sum

Decide whether some subset of [3,34,4,12,5,2] sums to 9, printing `yes`. A boolean Zig array swept in reverse marks every reachable subset total.

## Run

    zig run main.zig 2>&1
