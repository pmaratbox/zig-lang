# 0318 — Kth Largest

Find the 2nd largest element of [3,2,1,5,6,4] using a heap, printing `5`. In Zig a capped size-k min-heap keeps only the largest seen, so its root is the kth largest.

## Run

    zig run main.zig 2>&1
