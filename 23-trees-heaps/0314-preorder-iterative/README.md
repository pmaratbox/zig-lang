# 0314 — Iterative Preorder

Print the iterative (stack-based) preorder traversal of the BST built from 5,3,8,1,4 `5 3 1 4 8`. In Zig a fixed-size array with a top index serves as the explicit LIFO stack, pushing right before left.

## Run

    zig run main.zig 2>&1
