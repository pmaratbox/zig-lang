# 0316 — Max-Heap Pop All

Build a max-heap from [3,1,4,1,5], pop the top three values, printing `5 4 3`. In Zig a fixed-size array backs the binary heap with `std.mem.swap` driving the sift operations.

## Run

    zig run main.zig 2>&1
