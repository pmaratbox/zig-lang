# 0310 — BST Search

Insert 5,3,8,1,4 into a BST, then search for 4 (yes) and 6 (no), printing `yes no`. In Zig insertion walks a pointer-to-optional-pointer so the new node links in place.

## Run

    zig run main.zig 2>&1
