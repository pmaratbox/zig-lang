# 0311 — BST Delete

Insert 5,3,8,1,4 into a BST, delete 3, and print the in-order traversal `1 4 5 8`. In Zig deletion returns the replacement subtree pointer so the parent link is reassigned cleanly.

## Run

    zig run main.zig 2>&1
