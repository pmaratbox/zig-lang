# 0313 — Level-Order Traversal

Print the breadth-first (level-order) traversal of the BST built from 5,3,8,1,4 `5 3 8 1 4`. In Zig a fixed-size array with head/tail indices serves as the FIFO queue.

## Run

    zig run main.zig 2>&1
