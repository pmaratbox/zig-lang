# 0312 — Tree Height

Compute the height (in nodes) of the BST built from 5,3,8,1,4, printing `3`. In Zig `@max` over the recursive child heights keeps the formula compact.

## Run

    zig run main.zig 2>&1
